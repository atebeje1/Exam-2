import re

def countLexemes(file):
  out = 1 # Accounts for next line character
  for line in file:
    line = line.split(' ')
    for lexum in line:
      if lexum == re.match()
  return '# of lexums: {}'.format(out)

# ALlows for variable definition
